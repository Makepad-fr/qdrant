services:
  codegraph-qdrant:
    env_file:
      - ./envs/production/.env.qdrant
    volumes:
      - "${MAKEPAD_QDRANT_CODEGRAPH_DATA_PATH:-/var/lib/makepad/qdrant-codegraph}:/qdrant/storage"
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.labels.infra.makepad.qdrant == true
      restart_policy:
        condition: on-failure
        delay: 5s
      resources:
        limits:
          memory: 24G
        reservations:
          memory: 4G
      update_config:
        parallelism: 1
        order: stop-first
        failure_action: rollback
      rollback_config:
        parallelism: 1
        order: stop-first

  opsbrain-qdrant:
    env_file:
      - ./envs/production/.env.qdrant
    volumes:
      - "${MAKEPAD_QDRANT_OPSBRAIN_DATA_PATH:-/var/lib/makepad/qdrant-opsbrain}:/qdrant/storage"
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.labels.infra.makepad.qdrant == true
      restart_policy:
        condition: on-failure
        delay: 5s
      resources:
        limits:
          memory: 24G
        reservations:
          memory: 4G
      update_config:
        parallelism: 1
        order: stop-first
        failure_action: rollback
      rollback_config:
        parallelism: 1
        order: stop-first

networks:
  codegraph:
    external: true
    name: ${MAKEPAD_QDRANT_CODEGRAPH_NETWORK}
  opsbrain:
    external: true
    name: ${MAKEPAD_QDRANT_OPSBRAIN_NETWORK}
